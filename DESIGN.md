# Kanitsu产品与架构设计

> 更新于 2026-08-27。本文是当前有效的产品和工程约束，不记录已完成项目的实施过程。

## 1. 产品定位

Kanitsu是本地优先的图包管理和看图应用，主要面向 Windows 与 Android。用户选择源文件夹后，应用复制图片到自己的图包库；后续浏览、整理、重命名和删除只作用于这份副本。

核心原则：

- **本地优先**：默认不上传图片，不依赖在线服务。
- **副本隔离**：导入后不修改源文件夹，应用只管理自己的图库。
- **目录即图包**：每个目录都是可浏览、整理、导出和设置封面的相册节点。
- **跨端共享领域逻辑**：扫描、整理、封面与索引放在共享包，平台差异通过适配器隔离。
- **大库可用**：缩略图、虚拟化、预取和缓存是基础能力，不是后期补丁。

暂不支持 HEIC、TIFF、RAW 和在线同步。未来同步能力必须通过独立 provider 接入，不能侵入本地库核心流程。

## 2. 架构

```text
React UI
  ├─ Desktop: LibraryBrowser
  └─ Mobile:  MobileApp + MobileViewer
           │
Core / Organizer / Cover Picker
           │
ImportSourcePicker + LibraryStore
  ├─ Memory / Web demo
  ├─ Electron / Node.js
  └─ Android / Capacitor plugin
```

### 2.1 模块职责

| 模块 | 职责 |
|---|---|
| `apps/web` | Vite 入口、平台检测、主题与全局样式 |
| `apps/desktop` | Electron 窗口、系统对话框、文件协议、缩略图 worker、日志和打包 |
| `apps/mobile` | Capacitor 配置、Android Activity 与原生插件 |
| `packages/core` | 领域类型、路径、扫描、导入、整理落盘、删除和持久化索引 |
| `packages/fs-adapter` | 导入源与应用图库的统一接口及平台实现 |
| `packages/organizer` | 内置与自定义命名规则 |
| `packages/cover-picker` | 封面评分与用户固定封面优先级 |
| `packages/ui` | 桌面和触摸优先 UI、查看器、缩略图缓存及交互组件 |

桌面和移动端共享领域服务，但保留独立组件树。两端的导航、输入方式和信息密度差异较大，不强制复用页面组件。

### 2.2 平台抽象

平台能力收敛到两个接口：

- `ImportSourcePicker`：选择源目录、遍历源文件和读取导入内容。
- `LibraryStore`：管理应用图库、缩略图、原图 URL、移动删除、原生快速导入和 ZIP 导出。

平台专属行为必须放在适配器或应用壳内，共享包不得散布平台判断。完整契约以 [`packages/fs-adapter/src/types.ts`](packages/fs-adapter/src/types.ts) 为准。

## 3. 数据与路径

### 3.1 主要模型

| 模型 | 用途 |
|---|---|
| `FolderNode` | 目录树节点，记录直属与递归图片数 |
| `ImageEntry` | 图片路径、格式、大小、时间与可选尺寸 |
| `LibrarySnapshot` | 一次扫描得到的目录和图片索引 |
| `ImportTask` | 导入状态、计数、跳过项和错误 |
| `OrganizeBinding` | 图片到目标虚拟路径的整理映射 |
| `OrganizeManifest` | 整理落盘结果和撤销依据 |
| `ZipExportResult` | 跨平台导出结果 |

### 3.2 路径规则

- 库内相对路径统一使用 `/`，根路径为空字符串。
- ID 由规范化相对路径稳定生成；重命名或移动后通过重新扫描更新索引。
- 当前目录默认只展示直属图片；“包含子目录”模式显式聚合后代图片。
- 导入顶层目录重名时自动追加序号，任何用户输入都必须经过非法名称和路径逃逸校验。
- 平台文件操作必须确认目标位于应用图库根目录内。

### 3.3 存储

| 平台 | 图包库 | 索引 | 缩略图 |
|---|---|---|---|
| Electron | `userData/albums` | IndexedDB | `userData/thumbcache` + 内存缓存 |
| Android | 应用外部文件目录 `albums/` | WebView IndexedDB | 原生磁盘缓存 + 渲染端缓存 |
| Web demo | 内存 | 内存 | 内存 object URL |

图包库和缓存属于应用数据。导出文件由用户选择保存位置，不随应用数据管理。

## 4. 核心流程

### 4.1 导入

```text
选择源文件夹
  → 递归扫描并过滤支持格式
  → 复制到应用图库（保留目录结构）
  → 建立或刷新索引
  → 按优先级生成缩略图
  → 展示导入报告
```

支持格式：JPG、JPEG、PNG、WebP、AVIF、BMP、GIF。

要求：

- Electron 和 Android 优先使用原生整树复制，避免大文件通过 JS 桥往返。
- 单文件失败不应中断整个任务；错误和跳过项进入报告。
- 长任务提供进度与取消。取消后保留已复制内容，并允许重新扫描。
- 导入完成后释放源目录授权。

### 4.2 浏览与索引

- 启动时读取持久化快照，并用轻量指纹判断是否需要重扫。
- 所有增删改、整理和导入完成后刷新持久化索引。
- 目录树负责层级导航；内容区分别展示子目录和直属图片。
- 搜索、排序、列表/网格、聚合和多选只改变视图，不改变库结构。

### 4.3 自动整理

整理流程固定为“解析 → 预览 → 确认 → 落盘”：

1. 内置规则和用户正则规则解析文件名。
2. 生成虚拟目标路径和置信度，不立即修改文件。
3. 预览冲突、未分类项和重名策略。
4. 确认后创建目录并移动或重命名文件。
5. 保存 manifest，用于一键撤销。

整理只作用于应用图库。任何失败都必须可报告，不能静默覆盖已有文件。

### 4.4 封面

智能封面综合命名先验、尺寸、宽高比、大小和修改时间。用户固定的封面始终高于自动评分；封面失效时回退到智能选择。

### 4.5 导出

- 可导出整个库或任意目录。
- ZIP 保留当前目录结构，并包含 `index.json` 元数据。
- Electron 和 Android 使用原生流式输出，避免把完整 ZIP 放入渲染进程内存。
- 导出提供进度、取消和明确的成功或失败反馈。

## 5. 查看器与交互

### 5.1 桌面端

- 键盘左右键切图，上下键切同级目录，`Esc` 关闭。
- 支持缩放、平移、旋转、胶片条、鼠标侧键和相邻原图预取。
- 原图通过受控本地协议加载，不通过 IPC 复制大 Buffer。

### 5.2 移动端

- 左右滑动翻页，下滑关闭，双指缩放，放大后单指平移，双击切换适应与原始比例。
- 长图优先允许纵向查看，避免与关闭手势冲突。
- 系统返回键按浮层、查看器、目录的历史顺序逐级返回。
- App Bar、抽屉、动作面板、FAB 和安全区符合触摸优先布局。

### 5.3 图片加载与动画

- 当前图片先显示目标缩略图，原图在同一几何位置解码并覆盖。
- 缩略图保持不透明，原图单向淡入；不得同时淡出两层造成亮度闪烁。
- 图片加载替换只改变透明度，不能改变页面位置。
- 横向位移只用于用户触发的翻页；打开和关闭使用独立的轻量动画。
- 每次打开移动查看器使用独立会话，静止时只挂载当前页，避免复用上次内容。
- 所有非必要动画尊重 `prefers-reduced-motion`。

## 6. 性能约束

- 网格只挂载可视区域及少量 overscan，不能随图片总数线性增长 DOM。
- Electron 缩略图请求按“可见、滚动方向、当前目录、子目录、后台预热”排序；其他平台至少限制并发和预取规模。
- Electron 解码在 worker 中执行；Android 使用原生解码和磁盘缓存。
- object URL 和查看器页面缓存必须有上限，卸载时释放资源。
- 原图使用 URL 流式加载，相邻图只做有限预取。
- 大库滚动期间避免同步布局读取、无界动画和无界预加载。

## 7. 隐私与安全

- 默认不联网、不上传、不遥测图片内容。
- Android 使用 SAF 获取用户授权，不申请广泛存储权限。
- Electron 自定义协议和所有本地文件入口必须防止目录穿越。
- 删除、覆盖、批量整理等不可逆操作必须有确认或可撤销机制。
- 日志不得记录图片内容或敏感二进制数据；路径日志服从用户选择的日志等级。

## 8. 测试与验收

每次行为变更至少执行：

```bash
npm run typecheck
npm test --workspaces --if-present
```

涉及 Web 或移动 UI 时增加：

```bash
npm run build:web
npm run build:mobile
```

关键人工回归：

- 导入、取消、重名、跳过文件和导入报告。
- 整理预览、冲突、落盘和撤销。
- 删除、重命名、新建目录及索引刷新。
- 大图、GIF、长图、连续翻页、缩放和缩略图到原图过渡。
- Android 返回键、安全区、横竖屏和后台恢复。
- ZIP 解压、目录结构和 `index.json`。

## 9. 当前进度

已完成能力和下一阶段工作统一维护在 [开发进度与复盘](docs/开发进度与复盘.md)。Android 原生实现细节见 [Android 平台实现](docs/Android端设计.md)。
