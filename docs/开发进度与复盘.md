# 全能看图王：开发进度与复盘

> 记录当前实现状态、遇到的问题、解决方式，以及下一阶段开发准备。

---

## 1. 当前进度

### 已实现

> **近期新增**：缩略图生成迁移到 **4 个 worker 线程（sharp/libvips 主路径）**，并新增磁盘持久化缩略图缓存
> （`userData/thumbcache`）、**五级优先级队列**（可见 > 滚动方向 > 当前目录 > 子文件夹 > 全库预热）与启动全库预热；
> **动画 GIF 缩略图**（omggif 抽帧/自适应调色板/防残影/循环/采样倍率补偿，小 GIF 原样透传）。
> 查看器快速切换（缩略图占位 → 原图换入，无黑屏）、前进/后退导航（含鼠标侧键）、切换目录滚动位置记忆。
> 日志体系（主进程 UTF-8 日志文件 + 渲染端环形缓冲）与设置页重构（调试/缓存分页：日志等级、预取开关、清缓存）。
> 更早：daisyUI/Tailwind 全面重设计、自建标题栏与侧栏搜索、预览模糊、右键菜单、条目操作、设置页（整理规则管理）、
> 自定义正则整理规则、整理性能优化、持久化索引、命名整理落盘/撤销、zip 导出（附 `index.json`）、删除相册、
> 原图无损查看、键盘导航、面包屑、简体中文。
>
> **本轮（性能与动效里程碑，master ee9a658 合并）**：渲染层性能闭环——图片网格与文件夹卡片区窗口化虚拟化
> （可视行 ±2）、共享 IntersectionObserver 单例、object URL 小池；滚轮平滑滚动（零依赖：输入跟手 + 松手惯性 +
> 收尾吸附）、240Hz 屏掉帧对策（120Hz 内容帧率、行合成层、去强制布局、滚动时暂停加载动画）；缩略图链路修复
> （可见请求自动升级优先 0 插队、方向预取 recheck 抢占、重复请求乱序完成不毁缓存、大 GIF 缩略图 4MB 入缓存）；
> 生产构建改 `kanitu-app://` 协议（修复 file:// 白屏）；设置页新增帧率/掉帧/滚动写入监测面板；motion/mini 卡片
> spring 微交互与精致入场动画（滚动中自动禁用、尊重 prefers-reduced-motion）。

| 模块 | 状态 | 说明 |
|---|---|---|
| Monorepo 骨架 | ✅ | npm workspaces + TypeScript + Vite + React |
| 平台抽象 | ✅ | `ImportSourcePicker` / `LibraryStore` |
| Web 内存演示 | ✅ | `MemoryImportSourcePicker` / `MemoryLibraryStore` |
| Electron 导入 | ✅ | 系统目录选择 → 过滤图片 → 复制到应用图包目录 |
| Electron 图包库 | ✅ | `userData/albums` 下管理副本 |
| 目录树 | ✅ | 多层级、折叠/展开、点击父层级显示子层级 |
| 父层级计数 | ✅ | `imageCount` 为递归总数，`directImageCount` 为直属数 |
| 子文件夹封面 | ✅ | 父层级显示子文件夹卡片，封面由 `pickCover` 智能选择 |
| 直属图片网格 | ✅ | 当前目录只显示直属图片缩略图 |
| 智能封面算法 | ✅ | 命名先验 + 画质 + 时间新近 + 宽高比打分 |
| 命名整理预览 | ✅ | 按规则生成虚拟整理路径，不移动文件 |
| 自定义整理规则 | ✅ | 设置页管理：内置 Pixiv 编号分组等规则；支持添加/编辑/启停自定义正则规则（匹配去扩展名文件名 + `$1/$2…` 目标模板 + 置信度 + 测试预览），localStorage 持久化 |
| 整理落盘/撤销 | ✅ | `applyOrganize` 按绑定创建目录树、移动/重命名、冲突检测；`undoOrganize` 用 manifest 一键还原（平台无关，走 `LibraryStore`） |
| zip 导出 | ✅ | Electron 主进程 `archiver` 流式打包到保存对话框路径（原始文件、内存安全）；Web/Memory 用内置 STORE zip 编码器下载；保留目录结构 |
| 导出带索引 | ✅ | 导出 zip 附带 `index.json`（`{version, exportedAt, root, folders[], images[]}`） |
| 持久化索引 | ✅ | IndexedDB 缓存扫描结果，启动 `loadOrScan` 增量加载，变更后 `rescanLibrary` 刷新 |
| 删除相册 | ✅ | `deleteLibraryFolder` 删除选中目录及其全部下级（二次确认），禁止删除库根 |
| 原图无损查看 | ✅ | Electron `kanitu-file` 协议流式读取原始文件（取消 2560px 上限、避免大 Buffer）；查看器缩放/平移/旋转 + 相邻预取 |
| 键盘导航 | ✅ | 看图页内 ↑/↓ 切到同级目录并展示其首张图、←/→ 上一/下一张、Esc 关闭看图；目录树/网格视图不绑定快捷键 |
| 面包屑 | ✅ | 根到当前目录的可点击路径导航 |
| 导入报告 | ✅ | 显示跳过文件路径与原因 |
| 缩略图管线 | ✅ | 4 个 worker 线程（主路径 sharp/libvips，Node-API），主进程只做缓存与调度；worker 失败/冷门格式回退 `nativeImage`，失败文件黑名单避免反复尝试 |
| 磁盘缩略图缓存 | ✅ | `userData/thumbcache`：SHA1 键 = 路径+mtime+size+尺寸+版本，重启直接读盘免解码；1.5GB 上限按 mtime 清理最旧 |
| 优先级与预取 | ✅ | 五级队列：可见(0)>滚动方向(1)>当前目录(2)>子文件夹(3)>全库预热(4)；可见请求自动升级为优先 0 插队（不挂在慢速预取 FIFO 上）、方向预取 recheck:true 抢占下一屏（200ms 节流防重）；整目录预取并发 4；启动全库预热/子文件夹/封面弹窗逐级预取，可整体开关 |
| 动画 GIF 缩略图 | ✅ | 小 GIF（≤256KB）原样透传；大 GIF 由 worker 用 omggif 抽帧（≤16）→ 逐帧合成（按 disposal 防透明残影）→ median-cut 自适应调色板 → 可动/小体积/无限循环 GIF，延迟按采样倍率放大 |
| 查看器快速切换 | ✅ | 切换时先缩略图占位、原图后台解码完成后换入（无黑屏）；已解码相邻原图 LRU 像素池 + 错峰预解码 |
| 前进/后退导航 | ✅ | 栈+指针历史（`navHistory.ts`），页面按钮与鼠标侧键（X1/X2）均可触发；主进程 `will-navigate` 拦截误刷新 |
| 滚动位置记忆 | ✅ | 切换目录时按目录保存/恢复内容区滚动位置（rAF 节流）；恢复在虚拟化度量就绪后执行 |
| 网格虚拟化（P2） | ✅ | 图片网格与文件夹卡片区均窗口化：只挂载可视行 ±2（OVERSCAN 2），同构探测卡精确度量行高、绝对定位行撑起总高（DOM 5000→几十）；滚动重渲按窗口变化门控；侧栏目录树 React.memo |
| 共享 IO / URL 池（P1/P4） | ✅ | 模块级 IntersectionObserver 单例（visibleObserver.ts）；objectUrlPool 引用计数 + 64 空闲 URL LRU，正在展示的 URL 绝不撤销 |
| 滚轮平滑滚动 | ✅ | 零依赖 smoothScroll.ts：输入活跃期快速跟随（activeLerp 0.5）＋松手惯性收尾（lerp 0.3＋2px 吸附即停）；scrollTop 写入默认 120Hz（writeIntervalMs 8.3）；本地变量跟踪位置避免每帧强制布局 |
| 240Hz 掉帧对策 | ✅ | 行容器 will-change:transform 提升合成层；shimmer 改 ::before transform 平移（替代每帧重绘的 background-position 动画）并滚动时暂停；内容帧率 120Hz |
| 帧率与性能监测 | ✅ | 设置→调试新增“帧率与滚动性能”：rAF 帧间隔（帧率/平均/p95/掉帧/卡顿帧）、滚动回调耗时、滚动写入/秒；面板打开时自动采集、关闭自动停止 |
| 生产构建加载 | ✅ | vite `base: './'` + 主进程 `kanitu-app://` 协议（protocol.handle 服务 web/dist，显式 MIME、防目录穿越）；修复 loadFile(file://) 下绝对路径 404 与 module CORS 白屏 |
| 卡片动效 | ✅ | motion/mini（gzip +3.2kB）：hover/press spring 缩放；精致入场动画（淡入+上移 8px）仅在非滚动中挂载时播放（滚动中 `.sk-scrolling` 禁用），尊重 prefers-reduced-motion |
| 日志体系 | ✅ | 主进程日志写 UTF-8 文件（`userData/logs/kanitu-日期.log`，分级/时间；双模控制台：直连中文、管道 ASCII 安全）+ 渲染端环形缓冲（等级过滤 + 可插拔 sink，移动端可复用）；共享契约见 `packages/core/src/log.ts` |
| 设置页 | ✅ | 重构为 通用/整理规则/调试/缓存 四页签：调试含日志等级、预取开关、主进程/渲染端日志；缓存含渲染端+主进程统计与清缓存（渲染端 / 主进程内存+磁盘） |
| GIF | ✅ | 查看器保留原 GIF 动画读取；网格缩略图：小 GIF 原样透传，大 GIF 用 omggif 生成可动缩略图 |
| 运行模式标记 | ✅ | 侧边栏底部 + 设置页展示运行模式标签（Electron/Web 模式 + 版本 + daisyUI 版本），便于排查缓存 |
| 简体中文界面 | ✅ | 全流程 UI/弹窗/通知/系统对话框/错误提示已简体中文化（含整理冲突原因、未分类目录、导入来源默认名等） |
| 手动固定封面 | ✅ | 图片右键“设为封面”或图包右键“设置封面…”弹窗（层次化：子文件夹可进入挑选/直接用其封面，当前目录图片网格），写入 `pickCover` 的 `preferredId`（score=2, reason=user-pinned），本地 `kanitu-pinned-covers` 记忆，可取消；卡片显示 ⭐已固定 角标 |
| 右键菜单 | ✅ | 图片/图包右键菜单（`ContextMenu` + portal，边缘自适应）：图片查看/设封面/重命名/复制路径/删除；图包打开/新建子文件夹/展开收起/重命名/设置封面/整理/导出/复制路径/删除 |
| 条目操作 | ✅ | `packages/core/src/entry-ops.ts`：重命名图片/图包、删除图片、新建子文件夹，走 `LibraryStore`（平台无关），重名/空名/非法名校验 |
| 应用内弹框 | ✅ | 删除/重命名/新建统一改为应用内 Modal，去除系统原生 `confirm`/`prompt` |
| 主题切换 | ✅ | 深/浅色跟随 `prefers-color-scheme`，手动切换记忆到 `kanitu-theme`（`data-theme`） |
| 侧栏搜索 | ✅ | 相册/图片按名称实时过滤（大小写不敏感），空态区分“无匹配结果” |
| 预览模糊 | ✅ | 逐图绑定（`kanitu-blurred-images` 记忆），入口统一为右键菜单“设为/取消隐私预览”：图片项单张切换，图包项一键覆盖其全部子文件夹 |
| 自建标题栏 | ✅ | 移除原生顶栏，自建最小化/最大化/关闭（`window:*` IPC），标题栏区 `no-drag` 处理 |
| 整理性能优化 | ✅ | `applyOrganize` 增加文件夹/文件名单/文件引用三级缓存 + `onProgress`，避免逐文件重复扫描目录 |
| GIF 缩略图 | ✅ | 小 GIF（≤256KB）原样透传；大 GIF 在 worker 用 omggif 生成“可动且小”的动画缩略图（抽帧≤16、median-cut 自适应调色板、逐帧合成防残影、loop 无限、延迟按采样倍率补偿） |

### 未实现 / 下一阶段

| 模块 | 状态 | 说明 |
|---|---|---|
| Android SAF 导入 | ❌ | 尚未实现 Capacitor SAF 插件与 Android 复制 |
| HEIC/TIFF/RAW | ❌ | 暂不支持 |
| WebDAV 同步 | ❌ | 仅预留 `SyncProvider` 接口 |
| Windows 安装包 | ❌ | 尚未配置 electron-builder 打包签名 |
| 自动化测试 | 🚧 | core / fs-adapter 已有单元测试；尚无 UI / E2E 与 Electron 端自动化测试 |
| filmstrip 水平虚拟化 | 🚧 | 查看器底部缩略条仍全量渲染 images.map，方案同网格虚拟化 |
| IPC 预取洪峰治理 | 🚧 | 生产版采样显示 GPU/解码活跃（ImageDecodeTask 68/s）；评估整目录预取并发下调或滚动活跃时暂停当前目录预取 |

---

## 2. 关键问题复盘

### 2.1 Electron 二进制下载失败

**现象**：`npm install` 时 `node install.js` 报 `ECONNRESET`。

**解决**：在 `.npmrc` 中配置镜像：

```ini
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
```

**经验**：Electron 二进制必须走镜像，否则国内网络容易失败。

### 2.2 PowerShell 写入文件带 BOM

**现象**：Vite 启动时报 `Unexpected token '﻿'`，JSON 解析失败。

**原因**：PowerShell 5.1 的 `Set-Content -Encoding UTF8` 会写入 UTF-8 BOM。

**解决**：使用 `.NET` 写文件：

```powershell
$utf8NoBom = New-Object System.Text.UTF8Encoding($false)
[System.IO.File]::WriteAllText($path, $text, $utf8NoBom)
```

**经验**：在 Windows 环境中写源码文件应显式使用无 BOM UTF-8。

### 2.3 Vite 一直加载旧共享包代码

**现象**：App 版本标记已更新，但侧边栏 Tree 版本仍是旧版。

**根因**：之前 `tsc -p packages/*/tsconfig.json` 没有 `noEmit`，导致在 `packages/*/src` 下生成了一堆旧 `.js` 和 `.d.ts`。Vite 解析 `./LibraryBrowser` 时优先命中了旧的 `LibraryBrowser.js`，而不是新的 `LibraryBrowser.tsx`。

**解决**：

1. 删除所有 `packages/*/src` 下的 `.js` / `.d.ts`。
2. 所有 package tsconfig 增加 `"noEmit": true`。
3. 启动时清理 Electron session cache，并给 Vite URL 加 `?v=时间戳` 防缓存。

**经验**：源码目录中绝不能残留编译产物；typecheck 必须 `noEmit`。

### 2.4 父层级图片数为 0、点击无内容

**现象**：父文件夹只有子文件夹时显示 0 images，点击后 `No images in this folder`。

**原因**：之前 `imageCount` 和 `imagesOf` 只统计直属图片。

**解决**：

- `imageCount` 改为递归总数。
- `directImageCount` 记录直属图片数。
- `imagesOf` 保留递归版本用于封面选择。
- `directImagesOf` 用于当前文件夹网格，避免父层级加载全部子孙图片。

### 2.5 根目录加载几百张图导致卡顿

**现象**：进入应用根目录时递归加载所有图片。

**解决**：

- 当前文件夹只显示直属图片。
- 父层级改为“子文件夹卡片列表”。
- 每个子文件夹只加载一张 512px 封面。

### 2.6 大图导致 `Array buffer allocation failed`

**现象**：`library:readBlob` 大量报 `RangeError: Array buffer allocation failed`。

**原因**：每次缩略图/大图都通过 `fs.readFile` 读取完整原图，并复制 Buffer，内存爆炸。

**解决**：

- 新增 `library:readThumbnail`：`nativeImage` 解码后输出 512px JPEG。
- 网格和文件夹封面统一走缩略图。
- `library:readBlob` 对非 GIF 图片使用 `nativeImage` 解码并限制最大 2560px，不再 `fs.readFile` 完整大图。
- GIF 保留原文件读取以支持动画。

**经验**：Electron IPC 不适合传输大文件；缩略图和预览图必须在主进程原生生成。

### 2.7 滚轮"跳两次"与 240Hz 掉帧

**现象**：滚轮每滚一下列表跳两次；高刷屏（240Hz）上滚动持续掉帧（~100/s）。

**原因链**：

1. `scroll-behavior: smooth` 与原生滚轮叠加产生二次移动；
2. Chromium scroll anchoring 在虚拟化替换 DOM 时补偿滚动位置 → 第二次跳动；
3. 平滑循环每帧读 `el.scrollTop` 触发强制同步布局（240Hz 下跌 Active 秒级 = 主线程 Layout ~1s）；
4. 240Hz 屏每帧预算仅 4.17ms——每帧写 scrollTop + 行未提升合成层 + shimmer 每帧重绘，合成管线持续超载。

**解决**：

- 去掉 `scroll-smooth`；主滚动区 `overflow-anchor: none`；
- 平滑循环用本地变量跟踪位置（只在滚轮输入时读一次 DOM）；
- 行容器 `will-change: transform` 提升合成层；shimmer 改 `::before` transform 平移；滚动时暂停加载动画；
- 内容帧率 120Hz（writeIntervalMs 8.3）+ 收尾吸附（2px）+ 输入跟手（activeLerp 0.5）。

**经验**：滚轮类"顺滑"不能同时叠加浏览器机制；要么原生要么接管，接管时必须同时解决强制布局、合成层与帧预算。

### 2.8 生产构建白屏（file:// 与 Vite 产物）

**现象**：`npx electron .` 加载生产构建出现纯白窗口。

**原因**：`dist/index.html` 使用 `src="/assets/…"` 绝对路径（file:// 下变 `file:///assets` 404），且 `type="module"` 脚本在 file:// 下被 CORS 拦截（origin=null）。

**解决**：vite `base: './'` + 主进程注册 `kanitu-app` scheme（standard/secure/fetchAPI/cors），`protocol.handle` 服务 `web/dist`（显式 MIME、防目录穿越），生产分支 `loadURL('kanitu-app://bundle/index.html?v=…')`。

**经验**：Electron + Vite 生产加载优先自定义协议（VS Code/Obsidian 同款思路），不要依赖 loadFile。

### 2.9 DevTools trace 时间戳读取坑

**现象**：trace 显示"录制 12.5 小时"，大量闲置，与实际操作时长不符。

**原因**：`metadata` 类事件 `ts=0` 被当成时间锚点；本应剔除 `ph==='M'` 的事件后计算真实范围。绝对 ts（~45000 秒）是 Chromium 进程启动以来的微秒，不是录制时长。

**解决**：分析脚本一律 `events.filter(e => e.ph !== 'M' && e.ts > 1000)` 后再取 min/max；活动集中在录制末尾才是真实操作段。

**经验**：先验证时间轴再解读数据；掉帧/耗时归因前先清扫 metadata 事件。

### 2.10 渲染端缓存合并的隐藏副作用

**现象**：等待很久滚动仍重新载图；回看"已加载过"的图（多为 GIF）仍重载。

**原因**：`thumbnailCache` 对同一文件合并 Promise——可见请求会复用"整目录预取排在队尾"的慢 Promise，等于排队尾；主进程大 GIF 动画缩略图 >512KB 时渲染端直接不入缓存（条目生成即删）。重复请求（可见升级/方向预取/原预取）乱序完成时，失败分支会删除已出图条目、或把成功结果丢进垃圾桶。

**解决**：可见请求发现"已排队未出图"时发新优先 0 请求并替换缓存 Promise（200ms 节流）；方向预取 recheck:true；缓存语义改为"已出图条目任何失败不删除、成功结果在条目被清时自动重建"；GIF 缩略图上限 512KB→4MB（渲染端与主进程）。

**经验**：Promise 合并的"去重"会隐藏请求优先级；并发去重缓存必须显式定义乱序完成语义。

### 2.11 dev 下 StrictMode 双挂载导致卡片"白卡"

**现象**：dev 运行（React StrictMode）下快速滚动时卡片变白。

**原因**：入场动画先把卡片置 `opacity:0`，StrictMode 双挂载把动画 stop() 在透明处，第二次挂载又因滚动中跳过 → 内联透明度卡死在 0。

**解决**：入场逻辑所有跳过/清理路径复位内联样式；滚动中（`.sk-scrolling`，400ms 窗口）彻底禁用入场动画。

**经验**：dev（StrictMode）下任何"先隐藏再动画"的入场都要保证中断/重挂时不残留初始状态。

---

## 3. 当前架构速览

```
UI (React + Vite)
  ├─ LibraryBrowser（目录树 / 子文件夹卡片 / 直属图片网格 / 查看器 / 前进后退 / 滚动记忆 / 预取 /
  │           图片+文件夹双区虚拟化 / 滚轮平滑接线 / 滚动重渲门控 / 帧率监测上报）
  ├─ CardMotion（motion/mini：hover/press spring + 非滚动中才播的入场动画）
  ├─ ImportReport
  ├─ SettingsPage（通用 / 整理规则 / 调试 / 缓存）
  └─ Viewer（缩略图占位 → 原图换入、解码像素池、鼠标侧键）
        │
        ▼
Core Services
  ├─ importFolder / scanLibrary / loadOrScan / rescanLibrary
  ├─ organizeByFolder (虚拟预览) → applyOrganize / undoOrganize
  ├─ zipLibrary/.exportZip (导出)
  ├─ pickCover
  └─ log（跨端日志契约：等级/条目/时间格式）
        │
        ▼
FS Adapter / UI 缓存
  ├─ Memory* / Electron*（LibraryStore 平台抽象）
  ├─ thumbnailCache（渲染端 LRU 256MB：peek 命中即出图；可见请求自动升级、方向预取 recheck、
│           乱序完成不毁缓存；五级优先级常量）
  ├─ smoothScroll（滚轮平滑：输入跟手 + 松手惯性 + 收尾吸附，120Hz 写入防强制布局）
  ├─ fpsMonitor（rAF 帧间隔监测：fps/掉帧/滚动写入）
  ├─ visibleObserver（共享 IntersectionObserver 单例）/ objectUrlPool（URL 引用计数池）
  └─ debugLog（渲染端环形缓冲 + 等级过滤 + 可插拔 sink）
        │
        ▼
Electron Main
  ├─ dialog 选目录 / fs 复制扫描
  ├─ kanitu-app 协议（生产构建渲染层加载，修复 file:// 白屏）
  ├─ kanitu-file 协议（查看器原图流式读取）
  ├─ library:readThumbnail：内存缓存 → 磁盘缓存(thumbcache) → 五级优先级队列 → 4×worker(sharp/omggif) → nativeImage 回退
  ├─ library:readBlob（2560px 上限预览，非查看器路径）
  ├─ 缩略图磁盘/内存缓存 + 清理
  └─ 调试/日志 IPC（debug:thumbnailStats / setLevel / readLogs / cache:clear；logger 写 userData/logs）
```

---

## 4. 运行方式

### Web 演示

```bash
npm run dev:web
```

### Electron 开发

```bash
npm run dev:desktop
```

### 类型检查

```bash
npm run typecheck
```

---

## 5. 下一次开发建议顺序

1. **打包分发**
   - electron-builder 配置 NSIS 安装包
   - Capacitor Android APK/AAB

2. **Android SAF 导入**
   - Capacitor 插件：`ACTION_OPEN_DOCUMENT_TREE`
   - 复制到应用外部目录 `albums/`
   - 实现 Android `LibraryStore`

3. **Android zip 导出**
   - SAF 创建文件后写入用户指定位置

4. **查看器体验补强**
   - 手势（双击/双指捏合/边缘滑动）、共享元素过渡、EXIF 信息、旋转写回

5. **渲染层优化**
   - 网格虚拟化（windowing）与共享 IntersectionObserver 单例（详见 `docs/安卓相册性能优化借鉴.md`，P1/P2）

6. **增量扫描优化**
   - 持久化索引已接入；后续可做扫描进度、索引校验（etag/指纹）与崩溃恢复

## 6. 整理落盘：设计与测试说明

**语义**：在选中文件夹（容器）下，依据 `organizeByFolder` 生成的绑定，把目录子树内的文件移动到 `<容器>/<解析路径>`，创建缺失目录、检测冲突，并生成可撤销 manifest。低置信度（<0.5）的文件**不动**（保持保守）。

- `applyOrganize(store, snapshot, containerRelPath, bindings, options)`：返回 `{ manifest, conflicts, appliedCount, skippedLowConfidenceCount }`。冲突策略默认 `skip`（目标已存在则跳过并报告），可设 `rename` 生成 `xxx (2)`。
- `undoOrganize(store, manifest)`：按反向顺序把文件移回原处。
- 实现完全基于 `LibraryStore` 接口，Electron / Memory / 未来 Android 通用，无平台私有路径拼接（用 `ensureFolderRel` 从库根逐级 walk 创建/解析）。

**后续可做**：
- 冲突重命名策略做成 UI 可切换；整理后清理空目录。
- 置信度阈值暴露为设置项。
- 测试目前用 esbuild 打包后在 Node 直接运行（沙箱下 `node --test` 的子进程 spawn 受限，改为进程内执行）。

**验证**：`packages/core/test/organize.test.ts` 覆盖“落盘→re-scan 校验新树→撤销→re-scan 校验还原”与“target-exists 冲突报告”。

## 7. zip 导出：设计与测试说明

**双路径**（`zipLibrary(targetRelPath, onProgress)` 返回 `ZipExportResult`）：

- **Electron（主）**：renderer 调主进程 IPC `library:exportZip` → 主进程用 `archiver`（STORE，即 `zlib.level:0`）把 `userData/albums/<targetRelPath>` 流式写入**保存对话框**选择的路径。读取**原始文件**、边写边落盘，内存安全；zip 内目录结构与图包树一致（子目录导出时包一层自身名称，整库导出时顶层文件夹平铺）。
- **Web/Memory（演示）**：用内置纯 TS STORE zip 编码器（`fs-adapter/src/zip.ts`，无压缩、UTF-8 文件名、CRC32）生成 zip `Blob`，UI 触发浏览器下载。

**约定**：目标为 `''` 表示整库；目标为 `MangaA` 时 zip 里是 `MangaA/...`。

**注意事项**：
- `archiver` 无内置类型，已在 `apps/desktop/electron/archiver.d.ts` 提供最小声明；并加入 `apps/desktop` 的 `dependencies`（打包时需要，不能只作 electron-builder 的传递依赖）。
- Electron 端为流式写盘，大图包不整包进内存；Web 端目前整包在内存（可为后续优化）。

**验证**：`packages/fs-adapter/test/zip.test.ts`（`yauzl` 打开生成的 zip 校验条目）：整库目录结构、子目录包一层、空库空 zip，且校验内置 `index.json` 内容与结构，`npm run test -w @kanitu/fs-adapter`。

**导出带索引**：zip 内额外写入 `index.json`（`{ version, exportedAt, root, folders[], images[] }`）。Web/Memory 用 `fs-adapter/src/exportIndex.ts` 的 `buildExportIndexJson`；Electron 主进程用内联 `buildIndexJson` 生成同一 schema。索引条目不计入 `totalImages`。

## 8. 持久化索引与删除相册：设计与测试说明

**持久化索引**（`packages/core/src/library.ts` + `indexed.ts`）：
- `PersistentIndex` 接口（load/save/clear）+ 内存实现（演示兜底/测试）+ `createIdbPersistentIndex`（IndexedDB，出错优雅降级）。
- `loadOrScan(store, index)`：启动优先读缓存，未命中才 `scanLibrary` 并落盘 → 不再每次启动全量重扫。
- `rescanLibrary(store, index)`：导入/整理/撤销/删除/刷新后重扫并持久化。
- 仅在 Electron（持久库）启用 IDB 持久化；Web/Memory 演示用内存索引，避免跨会话读到过期数据（演示库是临时重生成的）。

**删除相册**（`packages/core/src/remove.ts`）：
- `deleteLibraryFolder(store, relPath)`：解析现有目录（不创建）→ `store.remove`（Memory 移除整棵子树 / Electron `fs.rm` recursive），禁止删除库根。
- UI 加 Delete Album 按钮，`window.confirm` 二次确认，删除后 `rescanLibrary` 刷新并跳到父级。

**验证**：`packages/core/test/library.test.ts`（loadOrScan 缓存命中/首次扫描、rescan 刷新、clear）、`packages/core/test/remove.test.ts`（删除整棵子树、拒绝删库根）。运行 `npm run test -w @kanitu/core`。

## 9. 原图无损查看 / 键盘导航 / 面包屑：设计与测试说明

**原图无损查看**：
- Electron 主进程注册 `kanitu-file://file/?p=<encodeURIComponent(abs path)>` 协议，handler 用 `assertInsideLibrary` 守卫后 `net.fetch(file URL)` 流式返回原始文件 → 查看器不再用 2560px 预览、也不经大 Buffer/IPC。
- `LibraryStore` 新增 `getViewerUrl`/`releaseViewerUrl`：Memory 返回 blob URL（需 revoke），Electron 返回协议 URL（无需释放）。
- 查看器（`Viewer`）改为原图 `<img>`：滚轮缩放、双击切 Fit/100%、拖拽平移、`R` 旋转、`+/−/0/1` 与 `←/→/Esc` 快捷键、相邻图片预取（加载后释放资源）。

**键盘导航**：仅在**看图页**生效——`ArrowUp/ArrowDown` 在当前目录父级的所有同层目录间切换并展示该目录**首张图**（仍在看图，无直属图的同级目录则回落其网格）；`ArrowLeft/ArrowRight` 在当前目录内上一/下一张；`Escape` 关闭看图回当前网格。目录树/网格视图**不绑定**快捷键。

**面包屑**：`crumbs` 沿 `parentId` 上溯得到根到当前目录链，content-header 顶部渲染可点击路径（末级高亮）。

**说明**：查看器/导航为纯 UI，经 `typecheck` 验证；核心逻辑（路径、同级计算）在 core/fs-adapter 层测试。Electron 协议路径需真机验证（本沙箱无法启动 Electron GUI）。

## 10. 右键菜单 / 条目操作 / 设置页 / 整理优化：设计与测试说明

**右键菜单**（`packages/ui/src/ContextMenu.tsx`）：
- 通用 `ContextMenu` 组件（React portal 到 `document.body`，边缘自动收敛到视口内，点击外部/Esc/滚轮/缩放关闭）。
- 图片右键：查看、设为封面/取消固定封面、重命名、复制路径、删除；图包右键：打开、新建子文件夹、展开/收起子目录、重命名、整理、导出 ZIP、复制路径、删除（库根不可重命名/删除）。
- 删除/重命名/新建均改为应用内 Modal（`promptState` 状态机），去除系统原生 `confirm`/`prompt`。

**条目操作**（`packages/core/src/entry-ops.ts`，平台无关）：
- `renameImage` / `renameFolder`：名称 trim、空名/路径分隔符校验、省略扩展名时自动补齐、同名冲突检测，走 `store.move`。
- `deleteImage`：按 `relPath` 解析文件引用后 `store.remove`。
- `createSubfolder`：在指定父目录新建空文件夹，重名报错。
- 辅助：`resolveFolderRef`（按库相对路径逐级解析，不创建）、`resolveImageFileRef`。

**固定封面**（`packages/cover-picker/src/coverPicker.ts`）：
- `pickCover` 新增 `preferredId` 参数，命中即返回 `{score: 2, reason: 'user-pinned'}`，优先于算法评分。
- UI 用 `kanitu-pinned-covers`（folderId → imageId）localStorage 记忆；设为封面/取消固定会立即刷新封面并通知。

**自定义整理规则**（`packages/organizer/src/organizer.ts` + `packages/ui/src/OrganizeRulesModal.tsx`）：
- `CustomOrganizeRule = { id, name, pattern, target, confidence, enabled }`：`pattern` 是匹配**去扩展名文件名**的正则，`target` 是目录模板（`$1/$2…` 替换捕获组），原文件名保留并放入结果目录。
- 自定义规则**优先于内置规则**按数组顺序匹配；无效正则/空目录/含 `..` 的路径被保守跳过。
- 设置页“整理规则”标签：展示内置规则（分隔符/日期/作者/卷话页/Pixiv 编号/标题+编号），自定义规则可增删改、启停、编辑时实时测试预览（示例文件名 → 虚拟路径）。
- 持久化到 localStorage `kanitu-organize-custom-rules`，启动时校验结构过滤非法项。

**整理性能优化**（`packages/core/src/organize.ts`）：
- `applyOrganize` 增加 `folderCache`（目录解析，逐级 memoize）、`fileNameCache`（目标目录直属文件名集合）、`fileRefCache`（直属文件引用），避免对每个绑定重复 `listChildren` 走目录。
- 原实现是 O(文件数 × 目录深度) 的重复扫描，现收敛为每个目录只列一次，大库整理显著提速。
- 新增 `options.onProgress(done, total)` 回调，UI 展示整理进度。

**GIF 缩略图**（`apps/desktop/electron/main.ts` + `thumbnailWorker.ts`）：
- 小 GIF（≤256KB）原样透传（动画完美、瞬时）；
- 大 GIF 由 worker 用 `omggif` 生成动画缩略图：抽帧 ≤16（均匀采样含首尾）→ 逐帧合成（按 `disposal` 清除，防透明残影）→ median-cut 自适应调色板（≤255 色）→ 编码为可动/小体积/无限循环 GIF，帧延迟按采样倍率放大以保持原循环时长；
- 超过 512KB 的条目（GIF 原样等）不进内存缓存、仅落盘，避免挤垮 LRU。

**验证**：
- `packages/core/test/organize.test.ts` 新增：Pixiv 编号分组、自定义规则优先于内置并按捕获组分组、无效正则被忽略（`applyCustomRule` 返回 null）。
- 条目操作/右键菜单为 UI 层，经 `typecheck` 验证；核心 `entry-ops`/`applyOrganize` 缓存逻辑由 `organize.test.ts` 的“落盘→re-scan 校验→撤销→re-scan 校验”与冲突用例间接覆盖。

## 11. 缩略图 / 缓存 / 日志体系：设计与验证

**缩略图生成（`apps/desktop/electron/main.ts` + `thumbnailWorker.ts`）**
- **worker 线程池（4 个）**：主路径 `sharp`（libvips，Node-API，与 Electron ABI 兼容，无需 rebuild），JPEG/PNG/WebP/AVIF/BMP 等在 worker 内解码→等比缩放 ≤512px→JPEG q80；worker 解析失败或冷门格式回退主进程 `nativeImage`，失败文件加入黑名单避免反复卡 worker；任务超时（8s）杀 worker 重建释放槽位。主进程事件循环不再被解码占用。
- **四级优先级队列**：`P0` 可见（插队最前）> `P1` 当前目录全量 > `P2` 子文件夹/封面 > `P3` 全库启动预热；worker 空闲总是从最高非空桶取任务。
- **GIF**：小（≤256KB）原样透传；大图用 `omggif` 生成动画缩略图（见 §10）。

**缓存（三层）**
- 磁盘：`userData/thumbcache/<SHA1>.jpg`，键 = `路径+mtime+size+目标尺寸+版本`（覆盖/移动自动失效），1.5GB 上限启动时按 mtime 清理最旧；
- 主进程内存 LRU（512MB，仅按容量）、渲染端 LRU（256MB，仅按容量）+ `peek` 同步命中即出图（不再闪加载条）；超大条目（>512KB）不入内存、仅落盘。

**预取（渲染端，`LibraryBrowser` / `CoverPickerModal` / `thumbnailCache`）**
- 启动全库预热（P3）、当前目录全量（P1）、子文件夹前 12 张（P2）、封面选择弹窗（直属 P1 / 子封面 P2）；低优先级、限并发 2、可取消；设置页“后台预取”开关可整体关闭做对照。

**日志与调试**
- 主进程 `logger.ts`：分级（debug/info/warn/error）、时间戳、写 UTF-8 文件 `userData/logs/kanitu-日期.log`；双模控制台（直连终端中文 + chcp 65001；管道转发输出 ASCII 安全版避免 GBK 乱码）。
- 渲染端 `debugLog.ts`：环形缓冲 300 条 + 等级过滤 + 可插拔 sink（移动端落盘/上报预留）；契约统一在 `packages/core/src/log.ts`。
- 设置页重构：**调试**页签（日志等级、预取开关、主进程日志文件、渲染端日志）、**缓存**页签（渲染端/主进程统计、清缓存：渲染端内存 / 主进程内存+磁盘）。
- 调试 IPC：`debug:thumbnailStats`（队列积压/worker/缓存/磁盘文件数）、`debug:setLevel`、`debug:readLogs`、`cache:clear`；启动时渲染端把保存的日志等级同步给主进程。

**验证**
- worker 基准：JPEG 12MP → 29ms、PNG 25MP → 162ms（vs 原纯 JS 秒级/超时）；GIF 动画缩略图端到端（loop=0、帧数≤16、颜色 ΔE≤7、透明合成无残影、帧延迟≈原帧长）；
- 冒烟测试（Node + worker_threads）覆盖 JPEG/PNG/GIF 解码→缩放→编码、缓存命中/peek/清空、滚动/统计；5 个 workspace `typecheck` + desktop/web 构建通过。